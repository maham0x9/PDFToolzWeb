<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PDF to Word Converter Online Free | Secure PDF to DOCX Tool - PDFToolz</title>
    <meta name="description"
        content="Convert PDF to Word instantly with our free PDF to Word converter online. Get 100% accurate, editable DOCX files with perfect layout preservation. No login required, secure PDF to Word conversion in your browser.">
    <meta name="keywords"
        content="PDF to Word converter online, free PDF to DOCX tool, secure PDF to Word conversion, convert PDF to Word, PDF to editable Word, online PDF converter">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/@phosphor-icons/web"></script>
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        brand: { 50: '#eff6ff', 100: '#dbeafe', 500: '#3b82f6', 600: '#2563eb', 700: '#1d4ed8', 900: '#1e3a8a' },
                        word: { 50: '#eff6ff', 100: '#dbeafe', 500: '#2b579a', 600: '#1e3a8a' }
                    },
                    animation: { 'pulse-slow': 'pulse 3s cubic-bezier(0.4, 0, 0.6, 1) infinite' }
                }
            }
        }
    </script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');

        body {
            font-family: 'Inter', sans-serif;
        }

        .drag-pattern {
            background-image: url("data:image/svg+xml,%3Csvg width='40' height='40' viewBox='0 0 40 40' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='%232b579a' fill-opacity='0.05' fill-rule='evenodd'%3E%3Cpath d='M0 40L40 0H20L0 20M40 40V20L20 40'/%3E%3C/g%3E%3C/svg%3E");
        }

        .drop-zone {
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .drop-zone.drag-active {
            border-color: #2b579a;
            background-color: #eff6ff;
            transform: scale(1.02);
        }

        .dark .drop-zone.drag-active {
            background-color: #1e293b;
        }

        @keyframes spin {
            to {
                transform: rotate(360deg);
            }
        }

        .spinner {
            animation: spin 1s linear infinite;
        }
    </style>
</head>

<body
    class="bg-slate-50 dark:bg-slate-900 text-slate-800 dark:text-slate-100 font-sans antialiased transition-colors duration-300">
    <!-- Navigation -->
    <nav
        class="sticky top-0 z-50 bg-white/95 dark:bg-slate-900/95 backdrop-blur-sm border-b border-slate-200 dark:border-slate-800">
        <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16 items-center">
                <div class="flex items-center gap-10">
                    <a href="index.html" class="flex-shrink-0 flex items-center gap-2 group">
                        <div
                            class="bg-brand-600 text-white p-1.5 rounded-lg group-hover:bg-brand-700 transition-colors">
                            <i class="ph-bold ph-files text-xl"></i>
                        </div>
                        <span class="text-xl font-bold tracking-tight text-slate-800 dark:text-white">PDF<span
                                class="text-brand-600 dark:text-brand-500">Toolz</span></span>
                    </a>
                    <div class="hidden md:flex space-x-4 items-center">
                        <span class="text-sm font-medium text-slate-400">/</span>
                        <span class="text-sm font-semibold text-slate-800 dark:text-slate-200">PDF to Word</span>
                    </div>
                </div>
                <div class="flex items-center space-x-4">
                    <button id="theme-toggle" onclick="toggleTheme()"
                        class="p-2 rounded-full text-slate-600 hover:bg-slate-100 dark:text-slate-300 dark:hover:bg-slate-800 transition-colors"><i
                            id="theme-icon" class="ph-bold ph-moon text-xl"></i></button>
                </div>
            </div>
        </div>
    </nav>

    <!-- Hero Section with Upload -->
    <section
        class="py-16 px-4 bg-gradient-to-b from-slate-50 to-white dark:from-slate-900 dark:to-slate-900 relative overflow-hidden">
        <div
            class="absolute top-0 left-1/4 w-96 h-96 bg-blue-100 dark:bg-blue-900/20 rounded-full blur-3xl animate-pulse-slow">
        </div>
        <div class="absolute bottom-0 right-1/4 w-96 h-96 bg-brand-100 dark:bg-brand-900/20 rounded-full blur-3xl animate-pulse-slow"
            style="animation-delay: 1.2s;"></div>

        <div class="max-w-4xl mx-auto text-center space-y-8 relative z-10">
            <!-- SEO Heading -->
            <div class="space-y-4">
                <h1 class="text-4xl md:text-5xl font-bold text-slate-900 dark:text-white tracking-tight">PDF to Word
                    Converter Online Free</h1>
                <p class="text-lg text-slate-600 dark:text-slate-400 max-w-2xl mx-auto leading-relaxed">Convert PDF to
                    editable Word documents with <strong>100% accurate</strong> layout preservation. <strong>No login
                        required</strong>, completely secure.</p>
            </div>

            <!-- Upload Section -->
            <div id="drop-zone"
                class="drop-zone bg-white dark:bg-slate-800 border-2 border-dashed border-slate-300 dark:border-slate-600 rounded-3xl p-12 md:p-16 shadow-xl cursor-pointer relative overflow-hidden group">
                <input type="file" id="file-input" class="absolute inset-0 w-full h-full opacity-0 cursor-pointer z-20"
                    accept=".pdf" />

                <div id="idle-content" class="relative z-10 flex flex-col items-center gap-6">
                    <div
                        class="w-24 h-24 bg-blue-50 dark:bg-slate-700 rounded-2xl flex items-center justify-center group-hover:scale-110 transition-transform">
                        <i class="ph-fill ph-microsoft-word-logo text-5xl text-word-500"></i>
                    </div>
                    <button
                        class="bg-brand-600 hover:bg-brand-700 text-white text-xl font-bold py-4 px-12 rounded-xl shadow-lg transition-all transform hover:-translate-y-1">Select
                        PDF file</button>
                    <p class="text-slate-400 dark:text-slate-500 text-sm font-medium">or drop PDF here</p>
                </div>

                <div id="success-content" class="hidden relative z-30 flex-col items-center gap-4 pointer-events-auto">
                    <div
                        class="w-20 h-20 bg-blue-50 dark:bg-blue-900/30 rounded-full flex items-center justify-center mb-2">
                        <i class="ph-bold ph-file-doc text-4xl text-word-500"></i>
                    </div>
                    <h3 class="text-xl font-bold text-slate-800 dark:text-white">File Ready to Convert</h3>
                    <p class="text-slate-500 dark:text-slate-400 text-sm font-medium bg-slate-100 dark:bg-slate-700 px-4 py-2 rounded-lg"
                        id="file-name"></p>

                    <div id="info-box"
                        class="mt-4 flex items-center gap-4 p-4 bg-blue-50 dark:bg-slate-700 border border-blue-100 dark:border-slate-600 rounded-xl text-sm text-blue-800 dark:text-blue-300">
                        <i class="ph-fill ph-magic-wand text-xl"></i><span>OCR will be applied automatically.</span>
                    </div>

                    <button id="convert-btn"
                        class="mt-6 bg-brand-600 hover:bg-brand-700 text-white text-lg font-bold py-3 px-8 rounded-xl shadow-lg transition-all transform hover:-translate-y-1 flex items-center gap-2 cursor-pointer relative z-50">
                        <span>Convert to WORD</span>
                        <i class="ph-bold ph-arrow-right"></i>
                    </button>

                    <button id="processing-btn"
                        class="hidden mt-6 bg-slate-400 cursor-not-allowed text-white text-lg font-bold py-3 px-8 rounded-xl flex items-center gap-3">
                        <i class="ph-bold ph-spinner spinner text-xl"></i>
                        <span>Converting...</span>
                    </button>

                    <a id="download-btn" href="#"
                        class="hidden mt-6 bg-green-600 hover:bg-green-700 text-white text-lg font-bold py-3 px-8 rounded-xl shadow-lg transition-all transform hover:-translate-y-1 flex items-center gap-2 relative z-50">
                        <i class="ph-bold ph-download-simple text-xl"></i>
                        <span>Download Word Doc</span>
                    </a>

                    <div class="max-w-6xl mx-auto px-4 text-center text-slate-500 dark:text-slate-400 text-sm">© 2025
                        PDFToolz Inc.
                        All rights reserved.</div>
                    </footer>

                    <script>
                        const dropZone = document.getElementById('drop-zone');
                        const fileInput = document.getElementById('file-input');
                        const idleContent = document.getElementById('idle-content');
                        const successContent = document.getElementById('success-content');
                        const fileNameDisplay = document.getElementById('file-name');
                        const convertBtn = document.getElementById('convert-btn');
                        const processingBtn = document.getElementById('processing-btn');
                        const downloadBtn = document.getElementById('download-btn');
                        const infoBox = document.getElementById('info-box');

                        let selectedFile = null;

                        // Drag & Drop Visuals
                        ['dragenter', 'dragover'].forEach(eventName => { dropZone.addEventListener(eventName, (e) => { e.preventDefault(); dropZone.classList.add('drag-active'); }, false); });
                        ['dragleave', 'drop'].forEach(eventName => { dropZone.addEventListener(eventName, (e) => { e.preventDefault(); dropZone.classList.remove('drag-active'); }, false); });

                        // Handle File Selection
                        const handleFile = (file) => {
                            if (file) {
                                selectedFile = file;
                                idleContent.classList.add('hidden');
                                successContent.classList.replace('hidden', 'flex');
                                fileNameDisplay.innerText = file.name;
                                fileInput.classList.add('hidden');
                            }
                        };

                        dropZone.addEventListener('drop', (e) => { handleFile(e.dataTransfer.files[0]); });
                        fileInput.addEventListener('change', (e) => { handleFile(e.target.files[0]); });

                        // Convert Logic
                        convertBtn.addEventListener('click', async () => {
                            if (!selectedFile) return;

                            convertBtn.classList.add('hidden');
                            processingBtn.classList.remove('hidden');
                            processingBtn.classList.add('flex');

                            const formData = new FormData();
                            formData.append('file', selectedFile);

                            try {
                                const response = await fetch('/convert-to-word', {
                                    method: 'POST',
                                    body: formData
                                });

                                const data = await response.json();

                                if (response.ok) {
                                    processingBtn.classList.add('hidden');
                                    processingBtn.classList.remove('flex');

                                    downloadBtn.href = data.download_url;
                                    downloadBtn.classList.remove('hidden');
                                    downloadBtn.classList.add('flex');

                                    infoBox.innerHTML = '<i class="ph-fill ph-check-circle text-xl text-green-600"></i><span class="text-green-700 dark:text-green-400">Conversion successful!</span>';
                                    infoBox.className = "mt-4 flex items-center gap-4 p-4 bg-green-50 dark:bg-green-900/20 border border-green-100 dark:border-green-800 rounded-xl text-sm";
                                } else {
                                    throw new Error(data.error || 'Conversion failed');
                                }

                            } catch (error) {
                                console.error(error);
                                processingBtn.classList.add('hidden');
                                processingBtn.classList.remove('flex');
                                convertBtn.classList.remove('hidden');
                                alert("Error converting file: " + error.message);
                            }
                        });

                        // Theme Logic
                        if (localStorage.theme === 'dark' || (!('theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) { document.documentElement.classList.add('dark'); updateIcon(true); } else { document.documentElement.classList.remove('dark'); updateIcon(false); }
                        function toggleTheme() { if (document.documentElement.classList.contains('dark')) { document.documentElement.classList.remove('dark'); localStorage.theme = 'light'; updateIcon(false); } else { document.documentElement.classList.add('dark'); localStorage.theme = 'dark'; updateIcon(true); } }
                        function updateIcon(isDark) { const icon = document.getElementById('theme-icon'); if (icon) { isDark ? icon.classList.replace('ph-moon', 'ph-sun') : icon.classList.replace('ph-sun', 'ph-moon'); } }
                    </script>
</body>

</html>